import 'package:flutter/material.dart';

class CEIALoading extends StatelessWidget {
  const CEIALoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
